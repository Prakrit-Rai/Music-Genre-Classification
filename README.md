# Music-Genre-Classification
Music genre classification project using the FMA dataset. Audio features extracted with Librosa are used to train and compare KNN, Random Forest, and SVM models. Performance is evaluated using accuracy, F1-score, and confusion matrices in a Jupyter Notebook environment.
